// small helper if you want to separate. Not necessary but included for clarity
const passport = require("passport");
const LocalStrategy = require("passport-local").Strategy;

module.exports = { passport, LocalStrategy };
